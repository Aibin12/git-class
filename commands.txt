commands for list config
 > git config --list

command for adding username
 > git config --global user.name "username"

command for adding email
 >git config --global user.email "email" 

commands for intializing git respository
 > git init 

commands for listing all untracked files
 > git status

commands for adding all untracked files 
 > git add -A 

command for adding all staged files to local respository
 > git commit -m "commit message"  



